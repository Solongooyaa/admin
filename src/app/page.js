"use client";
import Image from "next/image";

export default function Home() {
  let counter = 0;

  const handleClick = () => {};

  return (
    <div>
      <button onClick={handleClick}>click</button>
    </div>
  );
}
